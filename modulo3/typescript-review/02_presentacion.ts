// Declaración de variables con tipos explícitos
const nombre: string = "Heymi";
const edad: number = 20;
const ciudad: string = "Manabi";

// Impresión de la presentación usando template literals
console.log(`Me llamo ${nombre}.
Tengo ${edad} años.
Vivo en ${ciudad}.`);
